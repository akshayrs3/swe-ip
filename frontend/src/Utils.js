import Container from '@mui/material/Container';

// TODO: add an parameter to accept height
const VerticalSpace = () => {
  return (
    <Container sx={{ py: 2 }} maxWidth="md"></Container>
  );
};

export default VerticalSpace;